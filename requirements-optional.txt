# =============================================================================
# Parámetros por defecto (documentación; no los lee pip — úsalos en CLI o .env)
#   Multi-agente: 5 sub-agentes como máximo por defecto en la UI (Spinbox 1–5).
#   Memoria en blanco: cerebro recurrente W=0 aprendible (--blank-brain) y
#   núcleo SharedFlyMemory pequeño (slots aleatorios mínimos al crear la sesión).
#
#   N_AGENTES_DEFECTO=5
#   N_RONDAS_ORQUESTADOR=1
#   MEMORIA_N_SLOTS=8
#   MEMORIA_MEM_DIM=96
#   MEMORIA_MSG_DIM=40
#   MEMORIA_N_AGENTS_MAX=5
#   MEMORIA_LR=0.002
#   CEREBRO_BLANK_BRAIN=1
#   CEREBRO_FLY_LR=3e-5
#
#   Ejemplo chat:
#     python chat_fly_app.py --blank-brain --fly-lr 3e-5
# =============================================================================

# Dependencias opcionales (multi-agente avanzado, grafos, orquestación).
# No son necesarias para chat_fly_app.py ni el orquestador local con Ollama.
#
#   pip install -r requirements-optional.txt
#
langgraph>=0.2.55
langchain-core>=0.3.0
