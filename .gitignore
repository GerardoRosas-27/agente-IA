.env
.venv/
venv/
env/

__pycache__/
*.py[cod]
.Python
*.sqlite-journal

# Conectoma y base local (muy pesados; generar con download_connectome / prepare_flywire_data)
data/connections.csv
data/connections_princeton.csv.gz
data/plastic_swarm.sqlite

# Pesos y caché de modelos (LM Studio, Ollama, Hugging Face, etc.) — no subir al repo
models/
Modelos/
checkpoints/
*.gguf
*.ggml
*.gguf.*
*.safetensors
*.mlpackage
**/*.mlx
*.ckpt
*.h5
*.hdf5
*.npy

# Caché típica al descargar modelos (HF, transformers)
.cache/huggingface/
.cache/llm/
huggingface/

# LM Studio: carpeta de descargas (Windows/macOS: nombre puede variar)
# LM-Studio/
